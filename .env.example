# Borz Marketing Panel - Environment Configuration
#
# Copy this file to .env and fill in your credentials

# Generate a random secret key for session management
# You can use: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your_secret_key_here

# Discord Bot Token (for fetching user profiles and information)
# Get this from: https://discord.com/developers/applications -> Bot section
DISCORD_BOT_TOKEN=YOUR_BOT_TOKEN_HERE

# Discord OAuth2 Configuration (for account linking)
# Get these from: https://discord.com/developers/applications -> OAuth2 section
DISCORD_CLIENT_ID=your_client_id_here
DISCORD_CLIENT_SECRET=your_client_secret_here
# Set this to match your OAuth2 Redirect URI in Discord Developer Portal
# For local development: http://127.0.0.1:5000/discord/callback
# For production: https://adzsend.com/discord/callback
DISCORD_OAUTH_REDIRECT_URI=http://127.0.0.1:5000/discord/callback

# Resend API Key (for sending verification emails)
# Get this from: https://resend.com/api-keys
# RESEND_API_KEY=re_xxxxxxxxxxxx

# Email sender address (must be verified with Resend)
# EMAIL_FROM=noreply@yourdomain.com

# Bridge HMAC Secret Key (for signing Bridge secret keys)
# Generate a random 64-character hex key using:
# python -c "import secrets; print(secrets.token_hex(32))"
# IMPORTANT: Keep this secret! Changing it will invalidate all existing Bridge connections.
BRIDGE_HMAC_SECRET=your_bridge_hmac_secret_here
